# Web Channel — File Delivery

Images (`.png/.jpg/.jpeg/.gif/.webp/.svg`) render as inline previews in the browser.
Other files show as download links.

No size limit enforced by the channel itself (browser handles download).
